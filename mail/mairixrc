# mairix configuration file

# the base where our mail is stored, and where we put the mairix database
base=~/.mail

# Maildirs. '...' Means use all subfolders
maildir=ohm...
maildir=web...

# In my case uses around 35 MB
database=~/.mairix/database

# The output folder after a search
mfolder=mairix-search

# don't index spam folders
omit=web/Spam

# don't index trash folders
omit=ohm/trash
omit=web/Trash
